const NotFound = () => {
    return (
        <div className="error">
            <h1>Not Found!</h1>
            <p>
                Unfortunately, we could not find the requested page or resource.
            </p>
        </div>
    );
};

export default NotFound;
